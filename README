A. 
identifier for double average(double* n1,double &n2) is  _Z7averagePdRd

identifier for int average(int n1,float n2) is  _Z7averageif

B  
output:

	1 8
	4 8
	4 8
	8 8

char only use one byte so its size is 1. 
All memory takes 8 bytes in 64-bit system, so all pointers take 8 bytes.
int and float take 4 bytes and double takes 8 bytes.
